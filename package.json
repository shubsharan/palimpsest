{
  "name": "palimpsest",
  "version": "0.0.0",
  "private": true,
  "description": "Behavior-neutral three-agent runner for the Palimpsest distributed decipherment puzzle.",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "format": "oxfmt --config oxfmt.json --write README.md package.json tsconfig.json tsconfig.base.json oxfmt.json oxlint.json vitest.config.ts src tools tests specs docs fixtures/corpus/provenance.json AGENTS.md CLAUDE.md python/pyproject.toml && uv run --offline --frozen --project python ruff format --config python/pyproject.toml python",
    "format:check": "oxfmt --config oxfmt.json --check README.md package.json tsconfig.json tsconfig.base.json oxfmt.json oxlint.json vitest.config.ts src tools tests specs docs fixtures/corpus/provenance.json AGENTS.md CLAUDE.md python/pyproject.toml && uv run --offline --frozen --project python ruff format --config python/pyproject.toml --check python",
    "lint": "oxlint --deny-warnings src tools tests vitest.config.ts && uv run --offline --frozen --project python ruff check --config python/pyproject.toml python",
    "puzzle:build": "tsx src/cli.ts build",
    "puzzle:evaluate": "tsx src/cli.ts evaluate",
    "puzzle:offline": "tsx src/cli.ts offline",
    "puzzle:run": "tsx src/cli.ts run",
    "puzzle:sandbox:build": "tsx src/cli.ts sandbox-build",
    "test": "pnpm test:ts && pnpm test:py",
    "test:py": "uv run --offline --frozen --project python pytest -c python/pyproject.toml",
    "test:ts": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "verify": "pnpm verify:versions && pnpm format:check && pnpm lint && pnpm typecheck && pnpm test:ts && pnpm test:py",
    "verify:versions": "tsx tools/verify-versions.ts"
  },
  "devDependencies": {
    "@types/node": "26.1.1",
    "oxfmt": "0.60.0",
    "oxlint": "1.75.0",
    "tsx": "4.23.1",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  },
  "engines": {
    "node": "26.5.0",
    "pnpm": "10.14.0"
  },
  "packageManager": "pnpm@10.14.0"
}
