schemaVersion: 2
name: secret-label
models: { fixture: { provider: openai, model: fixture-model } }
runs:
  fixture:
    source: fixtures/corpus/fortunes-fool.txt
    agents: 2
    model: fixture
    communication: isolated
    releases: [0ms, 1ms]
    cutoff: 2ms
    spendCeilingCents: 0
    labels: { apiKey: literal-secret }
