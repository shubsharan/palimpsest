<!-- SPECKIT START -->

Current feature plan: `specs/019-configurable-run-controls/plan.md`.

For additional context about technologies to be used, project structure, shell commands, and other important information, read the current feature plan selected by `.specify/feature.json`.

Keep the runner local and research-focused: one strict experiment manifest, direct provider-neutral model sessions, deterministic Python puzzle construction, sequential attempts, all-canonical-origin evaluation, and durable attempt publication before optional overlap observation. Treat manifest schedules and resource policies as genuine per-run inputs: validate safe relationships, then freeze the resolved values in receipts, reservations, protocols, traces, and attempts instead of comparing them with one repository-wide experiment constant. Treat communication availability as a puzzle condition: preserve team identity and non-treatment inputs, expose ordinary peer Git only in shared conditions, and give isolated agents usable private Git without peer evidence or activity. Git commands remain model-chosen and unmetered. Agent-visible checking uses only the solver published to the assigned origin's `main` branch and exposes no correctness; post-freeze grading evaluates every canonical final origin without selection or repair. Keep CI advisory, and require a clean receipt-bound preflight before provider-backed research that spends money or may support findings.
<!-- SPECKIT END -->
