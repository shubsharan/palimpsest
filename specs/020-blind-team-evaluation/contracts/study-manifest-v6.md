# Contract: Study Manifest v6

The strict YAML manifest rejects unknown fields and defines one calibration study without phase selection.

```yaml
schemaVersion: 6

blocks:
  - blockId: calibration-odd-women
    sourcePath: fixtures/chronicles-of-break-oday.txt

order: [CS, CR, IR, IS]

checking:
  feedbackId: published-runnability-coverage-v1

scoring:
  primaryMetricId: normalized-positional-word-v1
  diagnosticMetricId: palimpsest-diagnostics-v1
  evaluationPolicyId: all-canonical-main-snapshots-v1
```

The manifest also freezes three GPT-5.6-sol medium-reasoning bindings, the one-hour schedule, null token ceilings, $10 per-attempt authorization, $40 total authorization, condition-native team channel availability, and the strict record identities. A future validation study uses a fresh manifest, sources, and receipt.
