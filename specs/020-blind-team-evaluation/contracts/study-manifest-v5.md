# Contract: Study Manifest v5

The strict YAML manifest rejects unknown fields and replaces reviewer selection with explicit checking and scoring policies.

```yaml
schemaVersion: 5

checking:
  feedbackId: published-runnability-coverage-v1

scoring:
  primaryMetricId: normalized-positional-word-v1
  diagnosticMetricId: palimpsest-diagnostics-v1
  evaluationPolicyId: all-canonical-main-snapshots-v1
```

The planned calibration also freezes the five active blocks in canonical order, calibration order `CS`, `CR`, `IR`, `IS`, three GPT-5.6-sol medium-reasoning bindings, the one-hour schedule, null token ceilings, $10 per-attempt authorization, $40 total authorization, condition-native team channel availability, and the advanced strict record identities.

Validation configuration remains present and frozen but no validation phase is launched by this feature.
